Given a number n, if at all it can be written as ab (b > 1), then b<log(n)+1. And for every fixed b, 
checking if there exists an a with ab=n can be done using binary search.
The total running time is therefore O(log2n) I guess.
